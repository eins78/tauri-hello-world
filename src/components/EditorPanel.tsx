import React from 'react'

export function EditorPanel({ ...restProps }: { style?: React.CSSProperties }) {
  return (
    <div {...restProps}>
      <div>
        <code>EditorPanel</code>
      </div>
    </div>
  )
}
