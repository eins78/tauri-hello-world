import React from 'react'

export function MetaDataPanel({ ...restProps }: { style?: React.CSSProperties }) {
  return (
    <div {...restProps}>
      <div>
        <code>MetaDataPanel</code>
      </div>
    </div>
  )
}
